### Instructions to use this project:

##### To configure:

`cmake --preset linux-debug`

##### To build the project:

`cmake --build --preset linux-debug`

##### To run generated executable:

`./out/build/linux-debug/bin/octoplayer`

##### To run unit tests:

`ctest --preset linux-debug`